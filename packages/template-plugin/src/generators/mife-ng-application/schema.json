{
  "$schema": "https://json-schema.org/schema",
  "$id": "MifeNgApplication",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the application.",
      "x-prompt": "What is the name of the application?"
    },
    "directory": {
      "type": "string",
      "description": "The relative directory where the application should be created.",
      "x-prompt": "In which relative directory should the application be created?"
    },
    "port": {
      "type": "number",
      "description": "The port to serve the application (should be uniq).",
      "x-prompt": "In which port should the application be served? (should be uniq)"
    }
  },
  "required": ["name", "directory", "port"]
}
